# Postgres DB URL (example)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/reviewsdb

# Optional Twilio settings (only needed if you verify signature or send messages)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# App settings
APP_HOST=0.0.0.0
APP_PORT=8000
STATE_TTL_SECONDS=1800   # time to expire incomplete conversations (seconds)
